<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
</head>
<body>
    <h1>My Profile</h1>
    <p><% if(user) { %>

        <img src="http://localhost:3000/<%= user.profile %>" alt="Profile" width="100" height="100">
        <h3>Name : <%= user.name %></h3>
        <h4>Name : <%= user.email %></h4>
        <h4>Role : <%= user.role %></h4>

        <a href="/logout">Logout</a>
        <% } %></p>
    
    
</body>
</html>